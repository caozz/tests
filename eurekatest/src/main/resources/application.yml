spring:
    application:
        name: eurekaServer
server:
    port: 8088
eureka:
    client:
        register-with-eureka: false
        fetch-registry: false
        service:
            defaultZone: http://localhost:8088/eureka
